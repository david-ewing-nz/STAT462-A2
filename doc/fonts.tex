% Load fontspec (required for xelatex and font control)
\usepackage{fontspec}

% Optional: define custom font command (not required if set via YAML)
% \newfontfamily{\arialfont}{Arial}

% Load additional packages as needed
\usepackage{makecell}  % Useful for flexible table cell formatting